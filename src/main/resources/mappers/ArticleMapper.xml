<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ymsoft.springdeveloper.com.springdeveloper.mapper.ArticleMapper">

    <resultMap id="articleMap" type="ymsoft.springdeveloper.com.springdeveloper.entity.Article">
        <id property="id" column="id"/>
        <result property="articleTitle" column="article_title"/>
        <result property="articleAuthor" column="article_author"/>
        <result property="articleDescription" column="article_description"/>
    </resultMap>

    <select id="findAll" resultMap="articleMap">
        SELECT * FROM article ORDER BY id DESC
    </select>

    <select id="findById" resultMap="articleMap">
        SELECT * FROM article WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article (article_title, article_author, article_description)
        VALUES (#{articleTitle}, #{articleAuthor}, #{articleDescription})
    </insert>

    <update id="update">
        UPDATE article SET
            article_title = #{articleTitle},
            article_author = #{articleAuthor},
            article_description = #{articleDescription}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM article WHERE id = #{id}
    </delete>

</mapper>
