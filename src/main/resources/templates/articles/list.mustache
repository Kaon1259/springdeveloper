{{>layouts/header}}

<div id="message-container">
    {{#message}}
        <div class="alert alert-success" role="alert">
            {{message}}
        </div>
    {{/message}}
</div>

<div class="container">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 20px;">
        <h2>📚 도서 목록</h2>
        <a href="/articles/new" class="btn btn-primary">+ 도서 등록</a>
    </div>

    <table class="table" style="margin-top: 20px;">
        <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">제목</th>
            <th scope="col">작가</th>
            <th scope="col">설명</th>
            <th scope="col">관리</th>
        </tr>
        </thead>
        <tbody>
        {{#articles}}
            <tr data-id="{{articleId}}">
                <th scope="row">{{articleId}}</th>
                <td>
                    <a href="/articles/{{articleId}}/edit" style="text-decoration:none; color:#007bff;">
                        {{articleTitle}}
                    </a>
                </td>
                <td>{{articleAuthor}}</td>
                <td>{{articleDescription}}</td>
                <td>
                    <button class="btn-delete btn btn-danger btn-sm">삭제</button>
                </td>
            </tr>
        {{/articles}}
        {{^articles}}
            <tr>
                <td colspan="5" style="text-align:center;">등록된 도서가 없습니다.</td>
            </tr>
        {{/articles}}
        </tbody>
    </table>
</div>
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script src="/js/article-delete.js"></script>

{{>layouts/footer}}
