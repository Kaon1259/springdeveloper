{{>layouts/header}}

    <style>
        body {
            background-color: #f5f7fb;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Noto Sans KR", Arial, sans-serif;
        }

        .hub-container {
            max-width: 960px;
            margin: 40px auto;
            padding: 0 16px;
        }

        .hub-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 24px;
        }

        .hub-title {
            font-size: 1.6rem;
            font-weight: 700;
            color: #222;
        }

        .hub-subtitle {
            font-size: 0.9rem;
            color: #777;
            margin-top: 4px;
        }

        .hub-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 16px;
        }

        .hub-card {
            background-color: #ffffff;
            border-radius: 16px;
            padding: 20px 18px;
            box-shadow: 0 6px 18px rgba(15, 23, 42, 0.08);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            min-height: 150px;
        }

        .hub-card-header {
            margin-bottom: 12px;
        }

        .hub-card-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .hub-card-tag {
            display: inline-flex;
            align-items: center;
            padding: 2px 10px;
            border-radius: 999px;
            font-size: 0.75rem;
            background-color: #eff6ff;
            color: #1d4ed8;
            font-weight: 500;
        }

        .hub-card-desc {
            font-size: 0.85rem;
            color: #555;
            margin-bottom: 14px;
            line-height: 1.5;
        }

        .hub-card-url {
            font-size: 0.75rem;
            color: #999;
            margin-bottom: 10px;
            word-break: break-all;
        }

        .hub-card-button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            border-radius: 999px;
            padding: 8px 12px;
            border: none;
            cursor: pointer;
            font-size: 0.9rem;
            font-weight: 600;
            background: linear-gradient(135deg, #2563eb, #4f46e5);
            color: #ffffff;
            text-decoration: none;
            transition: transform 0.08s ease-out, box-shadow 0.08s ease-out, filter 0.08s ease-out;
            box-shadow: 0 8px 15px rgba(37, 99, 235, 0.35);
        }

        .hub-card-button:hover {
            filter: brightness(1.02);
            transform: translateY(-1px);
            box-shadow: 0 10px 20px rgba(37, 99, 235, 0.45);
        }

        .hub-card-button:active {
            transform: translateY(0);
            box-shadow: 0 6px 12px rgba(37, 99, 235, 0.35);
        }

        .hub-card-button span {
            margin-left: 4px;
            font-size: 0.85rem;
        }

        @media (max-width: 480px) {
            .hub-title {
                font-size: 1.3rem;
            }
        }
    </style>
<div class="hub-container">
    <header class="hub-header">
        <div>
            <div class="hub-title">
                외부 시스템 허브
            </div>
            <div class="hub-subtitle">
                자주 사용하는 메가오더 · 쿠프마케팅 · 이지포스 웹을 한 곳에서 빠르게 접속합니다.
            </div>
        </div>

        {{#userName}}
            <div style="font-size:0.85rem; color:#555;">
                안녕하세요, <strong>{{userName}}</strong>님
            </div>
        {{/userName}}
    </header>

    <main class="hub-grid">

        <!-- 메가오더 -->
        <section class="hub-card">
            <div>
                <div class="hub-card-header">
                    <div class="hub-card-title">메가오더 (MegaOrder)</div>
                    <span class="hub-card-tag">주문/정산 어드민</span>
                </div>
                <p class="hub-card-desc">
                    매장 주문 현황 및 배달/정산 관련 데이터를 관리하는 메가오더 관리자 페이지입니다.
                </p>
                <p class="hub-card-url">
                    appadmin.annhouse.co.kr
                </p>
            </div>

            <a class="hub-card-button"
               href="https://appadmin.annhouse.co.kr"
               target="_blank"
               rel="noopener noreferrer">
                메가오더 열기
                <span>↗</span>
            </a>
        </section>

        <!-- 쿠프마케팅 -->
        <section class="hub-card">
            <div>
                <div class="hub-card-header">
                    <div class="hub-card-title">쿠프마케팅 (COOP Marketing)</div>
                    <span class="hub-card-tag">마케팅/포인트</span>
                </div>
                <p class="hub-card-desc">
                    포인트·스탬프·프로모션 등의 마케팅 데이터를 확인하고 설정하는 파트너 페이지입니다.
                </p>
                <p class="hub-card-url">
                    partner.coopnc.com
                </p>
            </div>

            <a class="hub-card-button"
               href="https://partner.coopnc.com"
               target="_blank"
               rel="noopener noreferrer">
                쿠프마케팅 열기
                <span>↗</span>
            </a>
        </section>

        <!-- 이지포스 웹 -->
        <section class="hub-card">
            <div>
                <div class="hub-card-header">
                    <div class="hub-card-title">이지포스 웹 (EasyPOS Web)</div>
                    <span class="hub-card-tag">POS 매출/메뉴</span>
                </div>
                <p class="hub-card-desc">
                    POS 매출, 영수증, 메뉴 정보를 웹에서 조회하고 관리하는 이지포스 웹 관리자입니다.
                </p>
                <p class="hub-card-url">
                    smart.easypos.net
                </p>
            </div>

            <a class="hub-card-button"
               href="https://smart.easypos.net"
               target="_blank"
               rel="noopener noreferrer">
                이지포스 웹 열기
                <span>↗</span>
            </a>
        </section>

    </main>
</div>

{{>layouts/footer}}