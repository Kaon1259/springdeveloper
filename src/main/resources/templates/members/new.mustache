{{>layouts/header}}

<!-- 간단 스타일 -->
<style>
    .text-mono { font-variant-numeric: tabular-nums; letter-spacing:-0.01em; }
    .wage-preview-header { background: linear-gradient(135deg, #198754 0%, #20c997 100%); color:#fff; border-bottom:none; }
    .wage-preview-card .table > :not(caption) > * > * { padding: 0.9rem 1rem; }
    .wage-preview-table thead th { background:#f6f9fb; color:#6c757d; font-weight:600; border-bottom:1px solid #e9ecef; }
    .wage-preview-table tbody tr:not(.table-subtle) td { border-top: 1px dashed #eef2f6; }
    .wage-preview-table .table-subtle td { background:#fafcfd; color:#7a8a99; }
    @media (min-width: 992px){ .sticky-side { position: sticky; top: 12px; } }
</style>

<main class="container my-4">
    <h2 class="mb-3">👤 아르바이트 회원 등록</h2>

    <form class="container px-0" action="/members/create" method="post" id="memberForm">
        <div class="row g-3">
            <!-- ============ 좌측: 기본 정보 ============ -->
            <div class="col-12 col-lg-6">
                <div class="card mb-3">
                    <!-- ✅ 상태를 헤더 오른쪽에 표시 -->
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <span class="fw-bold">기본 정보</span>
                        <select class="form-select form-select-sm w-auto" name="status" required>
                            <option value="">상태</option>
                            <option value="WAITING">시작전</option>
                            <option value="WORKING">근무중</option>
                            <option value="RESTING">휴식중</option>
                            <option value="PAUSED">일시중지</option>
                            <option value="RESIGNED">퇴사</option>
                        </select>
                    </div>

                    <div class="card-body">
                        <!-- 이름 -->
                        <div class="mb-3">
                            <label class="form-label">이름</label>
                            <input class="form-input form-control" type="text" name="name" placeholder="이름을 입력하세요" required>
                        </div>

                        <!-- 성별 -->
                        <div class="mb-3">
                            <label class="form-label">성별</label>
                            <select class="form-select" name="gender" required>
                                <option value="">성별을 선택하세요</option>
                                <option value="남성">남성</option>
                                <option value="여성">여성</option>
                            </select>
                        </div>

                        <!-- 연락처 -->
                        <div class="mb-3">
                            <label class="form-label">연락처</label>
                            <input
                                    class="form-input form-control"
                                    type="tel"
                                    name="phone"
                                    id="phone"
                                    placeholder="010-1234-5678"
                                    pattern="^0\d{1,2}-\d{3,4}-\d{4}$"
                                    autocomplete="tel"
                                    inputmode="tel"
                                    required>
                            <div class="form-text">숫자만 입력해도 자동으로 <code>010-1234-5678</code> 형태로 포맷팅됩니다.</div>
                        </div>

                        <!-- 이메일 -->
                        <div class="mb-3">
                            <label class="form-label">이메일</label>
                            <input class="form-input form-control" type="email" name="email" id="email" placeholder="name@example.com">
                            <div class="form-text">선택 입력입니다.</div>
                        </div>
                    </div>
                </div>

                <!-- 보건증 -->
                <div class="card mb-3">
                    <div class="card-header fw-bold">보건증</div>
                    <div class="card-body">
                        <!-- 보건증 보유 여부 -->
                        <div class="mb-3">
                            <label class="form-label d-block">보건증 보유</label>
                            <div class="form-check form-check-inline">
                                <input class="form-check-input" type="radio" name="hasHealthCertificate" value="true" id="healthYes">
                                <label class="form-check-label" for="healthYes">보유</label>
                            </div>
                            <div class="form-check form-check-inline">
                                <input class="form-check-input" type="radio" name="hasHealthCertificate" value="false" id="healthNo" checked>
                                <label class="form-check-label" for="healthNo">미보유</label>
                            </div>
                        </div>

                        <!-- 보건증 유효기간 -->
                        <div id="healthExpiryPanel" class="mt-2" style="display:none;">
                            <div class="d-flex align-items-end gap-2">
                                <div>
                                    <label class="form-label mb-1">유효기간(만료일)</label>
                                    <input type="date" class="form-input form-control" id="healthExpiryDate">
                                </div>
                                <div class="mb-2">
                                    <button type="button" class="btn btn-success btn-sm" id="applyHealthExpiry">적용</button>
                                    <button type="button" class="btn btn-secondary btn-sm" id="cancelHealthExpiry">취소</button>
                                </div>
                            </div>
                            <div id="healthExpiryPreview" class="text-muted mt-2">유효기간을 등록하세요.</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ============ 우측: 근무 조건/스케줄/예상급여 ============ -->
            <div class="col-12 col-lg-6">
                <div class="sticky-side d-grid gap-3">

                    <!-- 근무 조건 -->
                    <div class="card">
                        <div class="card-header fw-bold">근무 조건</div>
                        <div class="card-body">
                            <!-- 시작일 -->
                            <div class="mb-3">
                                <label class="form-label">아르바이트 시작일</label>
                                <input class="form-input form-control" type="date" name="startDate" required>
                            </div>

                            <!-- 시급 -->
                            <div class="mb-0">
                                <label class="form-label">시급(원)</label>
                                <input
                                        class="form-input form-control"
                                        type="number"
                                        name="hourlyWage"
                                        id="hourlyWage"
                                        min="0"
                                        step="100"
                                        placeholder="12000"
                                        required>
                                <div class="form-text">숫자만 입력 (예: 12000). 서버에서는 정수(원)로 저장됩니다.</div>
                            </div>
                        </div>
                    </div>

                    <!-- 근무시간 -->
                    <div class="card">
                        <div class="card-header fw-bold d-flex justify-content-between align-items-center">
                            <span>근무시간(요일/시간)</span>
                            <button type="button" class="btn btn-outline-primary btn-sm" id="openScheduleModal">등록/수정</button>
                        </div>
                        <div class="card-body">
                            <table class="table table-sm mb-0" id="scheduleTableView">
                                <thead>
                                <tr><th>요일</th><th>시작</th><th>종료</th></tr>
                                </thead>
                                <tbody>
                                <tr class="placeholder">
                                    <td colspan="3" class="text-muted">아직 등록된 요일/시간이 없습니다. “등록/수정” 버튼을 눌러 추가하세요.</td>
                                </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <!-- 예상 급여 -->
                    <div class="card wage-preview-card shadow-sm">
                        <div class="card-header wage-preview-header d-flex justify-content-between align-items-center">
                            <span class="fw-bold">예상 급여 미리보기</span>
                            <small class="text-white-50">스케줄/시급 변경 시 자동 반영</small>
                        </div>
                        <div class="card-body p-0">
                            <div class="table-responsive">
                                <table class="table table-borderless table-hover align-middle mb-0 wage-preview-table">
                                    <thead>
                                    <tr>
                                        <th class="w-50">항목</th>
                                        <th class="text-end w-50">값</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr>
                                        <td class="text-muted">주간 총 근무시간</td>
                                        <td class="text-end text-mono fw-semibold" id="previewWeeklyHours">0시간 0분</td>
                                    </tr>
                                    <tr>
                                        <td class="text-muted">주간 예상 급여</td>
                                        <td class="text-end text-mono fw-bold" id="previewWeeklyPay">₩0</td>
                                    </tr>
                                    <tr class="table-subtle">
                                        <td class="text-muted">월 환산 주차</td>
                                        <td class="text-end text-mono">× 4.345</td>
                                    </tr>
                                    <tr>
                                        <td class="text-muted">월간 예상 급여</td>
                                        <td class="text-end text-mono fw-bold text-success" id="previewMonthlyPay">₩0</td>
                                    </tr>
                                    </tbody>
                                </table>
                            </div>
                            <div class="px-3 pb-3 pt-2 small text-muted">* 월 환산은 평균 1개월 ≈ 4.345주 기준입니다.</div>
                        </div>
                    </div>

                    <!-- 등록 버튼 -->
                    <div class="d-grid">
                        <button type="submit" class="btn btn-success btn-lg">등록하기</button>
                    </div>
                </div>
            </div>
        </div>
    </form>
</main>

<!-- 모달: 요일/시간 등록 -->
<div class="modal" id="scheduleModal" style="display:none;">
    <div class="modal-dialog">
        <div class="modal-content p-3">
            <h3 class="mb-2">요일/시간 등록</h3>

            <div class="row mb-2">
                <div class="col">
                    <label class="form-label">요일</label>
                    <select class="form-select" id="daySelect">
                        <option value="MON">월요일</option>
                        <option value="TUE">화요일</option>
                        <option value="WED">수요일</option>
                        <option value="THU">목요일</option>
                        <option value="FRI">금요일</option>
                        <option value="SAT">토요일</option>
                        <option value="SUN">일요일</option>
                    </select>
                </div>
                <div class="col">
                    <label class="form-label">시작</label>
                    <input type="time" class="form-input form-control" id="startTime" value="10:00">
                </div>
                <div class="col">
                    <label class="form-label">종료</label>
                    <input type="time" class="form-input form-control" id="endTime" value="19:00">
                </div>
            </div>

            <div class="d-flex gap-2 mb-3">
                <button type="button" class="btn btn-primary" id="addScheduleRow">추가</button>
            </div>

            <table class="table table-sm mb-3" id="scheduleTableEdit">
                <thead><tr><th>요일</th><th>시작</th><th>종료</th><th>삭제</th></tr></thead>
                <tbody></tbody>
            </table>

            <div class="d-flex gap-2 justify-content-end">
                <button type="button" class="btn btn-success" id="applySchedule">적용</button>
                <button type="button" class="btn btn-secondary" id="cancelSchedule">취소</button>
            </div>
        </div>
    </div>
</div>

{{>layouts/footer}}

<!-- 스크립트 -->
<script>
    (function() {
        const form = document.getElementById('memberForm');

        /* ===== 전화번호 자동 포맷 (단일 백슬래시 정규식) ===== */
        const phone = document.getElementById('phone');
        function formatPhone(raw) {
            let v = String(raw || '').replace(/\D/g, '');
            if (!v) return '';
            if (v.startsWith('02')) {
                if (v.length <= 2) return v;
                if (v.length <= 6) return v.replace(/(02)(\d{0,4})/, '$1-$2');
                return v.replace(/(02)(\d{3,4})(\d{0,4}).*/, '$1-$2-$3');
            } else {
                if (v.length <= 3) return v;
                if (v.length <= 7) return v.replace(/(0\d{2})(\d{0,4})/, '$1-$2');
                return v.replace(/(0\d{2})(\d{3,4})(\d{0,4}).*/, '$1-$2-$3');
            }
        }
        function isValidPhone(s){ return /^(0\d{1,2})-\d{3,4}-\d{4}$/.test(s); }

        phone.addEventListener('input', () => {
            const pos = phone.selectionStart;
            const before = phone.value.length;
            phone.value = formatPhone(phone.value);
            const after = phone.value.length;
            const delta = after - before;
            const next = Math.max(0, (pos || 0) + delta);
            phone.setSelectionRange(next, next);
            phone.setCustomValidity('');
        });
        phone.addEventListener('blur', () => {
            phone.value = formatPhone(phone.value);
            if (phone.value && !isValidPhone(phone.value)) {
                phone.setCustomValidity('예: 010-1234-5678 형식으로 입력하세요.');
            } else {
                phone.setCustomValidity('');
            }
        });

        /* ===== 시급 변경 시 미리보기 갱신 ===== */
        const hourlyWage = document.getElementById('hourlyWage');
        hourlyWage.addEventListener('blur', updatePreview);
        hourlyWage.addEventListener('input', updatePreview);

        /* ===== 스케줄 관련 ===== */
        const viewTableBody = document.querySelector('#scheduleTableView tbody');
        const editTableBody = document.querySelector('#scheduleTableEdit tbody');
        const openBtn = document.getElementById('openScheduleModal');
        const modal = document.getElementById('scheduleModal');
        const scheduleApplyBtn = document.getElementById('applySchedule');
        const scheduleCancelBtn = document.getElementById('cancelSchedule');
        const addRowBtn = document.getElementById('addScheduleRow');
        const daySelect = document.getElementById('daySelect');
        const startInput = document.getElementById('startTime');
        const endInput = document.getElementById('endTime');
        const dayMap = { MON:'월', TUE:'화', WED:'수', THU:'목', FRI:'금', SAT:'토', SUN:'일' };

        openBtn.addEventListener('click', () => {
            editTableBody.innerHTML = '';
            Array.from(viewTableBody.querySelectorAll('tr')).forEach(tr => {
                if (tr.classList.contains('placeholder')) return;
                const d = tr.querySelector('td:nth-child(1)').getAttribute('data-day');
                const s = tr.querySelector('td:nth-child(2)').getAttribute('data-start');
                const e = tr.querySelector('td:nth-child(3)').getAttribute('data-end');
                appendEditRow(d, s, e);
            });
            modal.style.display = 'block';
        });

        addRowBtn.addEventListener('click', () => {
            const day = daySelect.value;
            const start = startInput.value;
            const end = endInput.value;
            if (!start || !end) { alert('시간을 입력하세요.'); return; }
            if (start >= end) { alert('종료 시간이 시작 시간보다 커야 합니다.'); return; }
            appendEditRow(day, start, end);
        });

        editTableBody.addEventListener('click', (e) => {
            if (e.target && e.target.classList.contains('remove-row')) {
                e.target.closest('tr').remove();
                updatePreview();
            }
        });

        scheduleApplyBtn.addEventListener('click', () => {
            viewTableBody.innerHTML = '';
            const rows = Array.from(editTableBody.querySelectorAll('tr'));
            if (rows.length === 0) addViewPlaceholder();
            else rows.forEach(r => {
                const d = r.querySelector('td:nth-child(1)').getAttribute('data-day');
                const s = r.querySelector('td:nth-child(2)').getAttribute('data-start');
                const e = r.querySelector('td:nth-child(3)').getAttribute('data-end');
                appendViewRow(d, s, e);
            });
            rebuildHiddenInputsFromView();
            modal.style.display = 'none';
            updatePreview();
        });

        scheduleCancelBtn.addEventListener('click', () => { modal.style.display = 'none'; });

        function appendEditRow(day, start, end) {
            const tr = document.createElement('tr');
            tr.innerHTML = `
        <td data-day="${day}">${dayMap[day]}(${day})</td>
        <td data-start="${start}">${start}</td>
        <td data-end="${end}">${end}</td>
        <td><button type="button" class="btn btn-link text-danger btn-sm remove-row">삭제</button></td>`;
            editTableBody.appendChild(tr);
        }

        function appendViewRow(day, start, end) {
            const tr = document.createElement('tr');
            tr.innerHTML = `
        <td data-day="${day}">${dayMap[day]}(${day})</td>
        <td data-start="${start}">${start}</td>
        <td data-end="${end}">${end}</td>`;
            viewTableBody.appendChild(tr);
        }

        function addViewPlaceholder() {
            const tr = document.createElement('tr');
            tr.className = 'placeholder';
            tr.innerHTML = `<td colspan="3" class="text-muted">아직 등록된 요일/시간이 없습니다.</td>`;
            viewTableBody.appendChild(tr);
        }

        function rebuildHiddenInputsFromView() {
            form.querySelectorAll('input[data-schedule="1"]').forEach(n => n.remove());
            const rows = Array.from(viewTableBody.querySelectorAll('tr'))
                    .filter(tr => !tr.classList.contains('placeholder'));
            rows.forEach((r, i) => {
                const day = r.querySelector('td:nth-child(1)').getAttribute('data-day');
                const start = r.querySelector('td:nth-child(2)').getAttribute('data-start');
                const end = r.querySelector('td:nth-child(3)').getAttribute('data-end');
                addHidden(`schedule[${i}].day`, day);
                addHidden(`schedule[${i}].start`, start);
                addHidden(`schedule[${i}].end`, end);
            });
        }

        function addHidden(name, value) {
            const input = document.createElement('input');
            input.type = 'hidden';
            input.name = name;
            input.value = value;
            input.setAttribute('data-schedule', '1');
            form.appendChild(input);
        }

        /* ===== 보건증 인라인 폼 ===== */
        const healthYes = document.getElementById('healthYes');
        const healthNo  = document.getElementById('healthNo');
        const panel     = document.getElementById('healthExpiryPanel');
        const dateInput = document.getElementById('healthExpiryDate');
        const applyHealthBtn  = document.getElementById('applyHealthExpiry');
        const cancelHealthBtn = document.getElementById('cancelHealthExpiry');
        const preview   = document.getElementById('healthExpiryPreview');
        const HIDDEN_FLAG = 'data-health-expiry';

        function onHealthRadioChange() {
            if (healthYes.checked) {
                panel.style.display = 'block';
                const existing = form.querySelector('input[type="hidden"][name="healthCertExpiry"]');
                if (existing && existing.value) {
                    dateInput.value = existing.value;
                    preview.textContent = `만료일: ${existing.value}`;
                    preview.classList.remove('text-muted');
                } else {
                    dateInput.value = '';
                    preview.textContent = '유효기간을 등록하세요.';
                    preview.classList.add('text-muted');
                }
            } else {
                panel.style.display = 'none';
                removeHealthExpiryHidden();
                dateInput.value = '';
                preview.textContent = '보유 선택 시 유효기간을 등록할 수 있습니다.';
                preview.classList.add('text-muted');
            }
        }
        healthYes.addEventListener('change', onHealthRadioChange);
        healthNo.addEventListener('change', onHealthRadioChange);

        applyHealthBtn.addEventListener('click', () => {
            const val = dateInput.value;
            if (!val) { alert('유효기간(만료일)을 선택하세요.'); return; }
            const today = new Date().toISOString().slice(0,10);
            if (val < today && !confirm('선택한 날짜가 이미 지났습니다. 그대로 적용할까요?')) return;

            removeHealthExpiryHidden();
            const hidden = document.createElement('input');
            hidden.type = 'hidden';
            hidden.name = 'healthCertExpiry';
            hidden.value = val;
            hidden.setAttribute(HIDDEN_FLAG, '1'); // data-health-expiry="1"
            form.appendChild(hidden);

            preview.textContent = `유효기간: ${val}`;
            preview.classList.remove('text-muted');
        });

        cancelHealthBtn.addEventListener('click', () => {
            const existing = form.querySelector('input[type="hidden"][name="healthCertExpiry"]');
            if (existing && existing.value) {
                dateInput.value = existing.value;
                preview.textContent = `유효기간: ${existing.value}`;
                preview.classList.remove('text-muted');
            } else {
                dateInput.value = '';
                preview.textContent = '유효기간을 등록하세요.';
                preview.classList.add('text-muted');
            }
        });

        function removeHealthExpiryHidden() {
            form.querySelectorAll(`input[${HIDDEN_FLAG}="1"]`).forEach(n => n.remove());
        }

        /* ===== 제출 전 최종 검증 ===== */
        form.addEventListener('submit', (e) => {
            // 전화번호 최종 보정 및 확인
            phone.value = formatPhone(phone.value);
            if (!isValidPhone(phone.value)) {
                e.preventDefault();
                phone.setCustomValidity('예: 010-1234-5678 형식으로 입력하세요.');
                phone.reportValidity();
                return;
            } else {
                phone.setCustomValidity('');
            }

            // 보건증 유효기간 경고
            if (healthYes.checked) {
                const hidden = form.querySelector('input[name="healthCertExpiry"]');
                if (!hidden && !confirm('보건증 “보유”로 선택하셨습니다. 유효기간 없이 계속 진행할까요?')) {
                    e.preventDefault();
                    return;
                }
            }
            // 스케줄 최신화
            rebuildHiddenInputsFromView();
        });

        onHealthRadioChange();

        /* ======== ▶ 예상 급여 미리보기 계산 로직 ◀ ======== */
        const weeklyHoursEl  = document.getElementById('previewWeeklyHours');
        const weeklyPayEl    = document.getElementById('previewWeeklyPay');
        const monthlyPayEl   = document.getElementById('previewMonthlyPay');

        // View 테이블 변화 자동 감지 → 미리보기 갱신
        const viewObserver = new MutationObserver(() => updatePreview());
        viewObserver.observe(viewTableBody, { childList: true });

        function updatePreview() {
            const totalMin = computeWeeklyMinutesFromView();
            const wage = parseInt(String(hourlyWage.value || '0').replace(/,/g,''), 10) || 0;

            const hours = Math.floor(totalMin / 60);
            const mins  = totalMin % 60;
            weeklyHoursEl.textContent = `${hours}시간 ${mins}분`;

            const weeklyPay = Math.round((totalMin / 60) * wage);
            weeklyPayEl.textContent  = formatKRW(weeklyPay);

            const monthlyPay = Math.round(weeklyPay * 4.345); // 평균 1개월 ≈ 4.345주
            monthlyPayEl.textContent = formatKRW(monthlyPay);
        }

        function computeWeeklyMinutesFromView() {
            const rows = Array.from(viewTableBody.querySelectorAll('tr')).filter(tr => !tr.classList.contains('placeholder'));
            let total = 0;
            rows.forEach(r => {
                const start = r.querySelector('td:nth-child(2)').getAttribute('data-start'); // "HH:MM"
                const end   = r.querySelector('td:nth-child(3)').getAttribute('data-end');   // "HH:MM"
                total += diffMinutes(start, end);
            });
            return total;
        }

        function diffMinutes(start, end) {
            if (!start || !end) return 0;
            const [sh, sm] = start.split(':').map(n => parseInt(n,10));
            const [eh, em] = end.split(':').map(n => parseInt(n,10));
            return (eh*60 + em) - (sh*60 + sm);
        }

        function formatKRW(n) {
            return new Intl.NumberFormat('ko-KR', { style: 'currency', currency: 'KRW', maximumFractionDigits: 0 }).format(n || 0);
        }

        // 초기 미리보기 표시
        updatePreview();
    })();
</script>

{{>layouts/footer}}
