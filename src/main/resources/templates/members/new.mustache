{{>layouts/header}}
{{>layouts/toast}}

<main class="schedule-shell py-4">
    <div class="schedule-layout container">

        <!-- ============ 상단 파란 패널 + 아래 전체 폼/테이블을 하나의 패널로 합침 ============ -->
        <section class="panel panel-merged mb-3">

            <!-- 🔵 상단: 아르바이트 정보 입력 헤더 (status-panel-blue 스타일 적용) -->
            <div class="member-header-blue status-panel-blue">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="d-flex align-items-center gap-2">
                            <span class="title-sm text-light-emphasis">STAFF CREATE</span>
                        </div>

                        <div>
                            <div class="status-title mt-1">
                                아르바이트 정보 입력
                            </div>
                            <div class="status-subtitle-small">
                                기본 인적사항과 급여 정보, 주간 템플릿까지 한 번에 등록할 수 있습니다.
                            </div>
                        </div>

                        <!-- ✅ 신규등록 텍스트 바로 아래: 상태 + 이름 (파란 배경 영역 안) -->
                        <div class="mt-3 d-flex flex-wrap align-items-end gap-2 status-inline-row">
                            <div class="d-flex flex-column">
                                <span class="status-period-label-caption mb-1">상태</span>
                                <select id="statusSelectTop"
                                        class="form-select form-select-sm status-select-top"
                                        name="status"
                                        form="memberEditForm"
                                        required>
                                    <option value="WAITING">시작전</option>
                                    <option value="WORKING">근무중</option>
                                    <option value="RESTING">휴식중</option>
                                    <option value="PAUSED">일시중지</option>
                                    <option value="RESIGNED">퇴사</option>
                                </select>
                            </div>

                            <div class="d-flex flex-column">
                                <span class="status-period-label-caption mb-1">이름</span>
                                <input type="text"
                                       class="form-control form-control-sm status-name-input"
                                       name="name"
                                       form="memberEditForm"
                                       placeholder="이름 입력"
                                       required>
                            </div>
                        </div>
                    </div>

                    <!-- 오른쪽: 저장하기 버튼만 -->
                    <div class="d-flex flex-column align-items-end gap-2">
                        <button type="submit"
                                form="memberEditForm"
                                class="btn btn-sm btn-light fw-semibold shadow-sm member-save-btn">
                            저장하기
                        </button>
                    </div>
                </div>
            </div>

            <!-- ============ 아래: 아르바이트 입력정보 + 주간 템플릿 (하나의 패널-바디 안에 배치) ============ -->
            <div class="panel-body panel-body-merged">

                <!-- 🔹 action 은 프로젝트에 맞게 변경 -->
                <form id="memberEditForm" method="post" action="/members/create" autocomplete="off">

                    <!-- ✅ 1줄: 성별 / 이메일 / 전화번호 / 주민등록번호 -->
                    <div class="info-grid mb-3">
                        <div class="info-item">
                            <div class="label">성별</div>
                            <select class="form-select form-select-sm"
                                    name="gender"
                                    required>
                                <option value="">선택</option>
                                <option value="M">남</option>
                                <option value="F">여</option>
                            </select>
                        </div>

                        <div class="info-item">
                            <div class="label">이메일</div>
                            <input type="email"
                                   class="form-control form-control-sm"
                                   name="email"
                                   value="">
                        </div>

                        <div class="info-item">
                            <div class="label">전화번호</div>
                            <input type="text"
                                   class="form-control form-control-sm"
                                   name="phone"
                                   id="phone"
                                   placeholder="010-0000-0000"
                                   value="">
                        </div>

                        <!-- 🔹 주민등록번호 입력 (ssn 필드로 전송) -->
                        <div class="info-item">
                            <div class="label">주민등록번호</div>
                            <input type="text"
                                   class="form-control form-control-sm"
                                   name="ssn"
                                   id="ssn"
                                   placeholder="000000-0000000"
                                   value="">
                        </div>
                    </div>

                    <!-- 2줄: 시작일 / 시급 2종 / 주휴수당 적용 -->
                    <div class="info-grid mb-3">
                        <div class="info-item">
                            <div class="label">시작일(근무)</div>
                            <input type="date"
                                   class="form-control form-control-sm"
                                   name="startDate"
                                   value="">
                        </div>

                        <!-- ✅ 기존 시급 라벨 수정: 시급(원: >=15h) -->
                        <div class="info-item">
                            <div class="label">시급(원: 근무시간&gt;=15h)</div>
                            <input type="number"
                                   min="0"
                                   step="10"
                                   class="form-control form-control-sm text-end"
                                   name="hourlyWage"
                                   placeholder="10030"
                                   value="">
                        </div>

                        <!-- ✅ 신규 시급 필드: 시급(원: 15h>) / name=hourlyWage2 -->
                        <div class="info-item">
                            <div class="label">시급(원: 15h&gt;근무시간)</div>
                            <input type="number"
                                   min="0"
                                   step="100"
                                   class="form-control form-control-sm text-end"
                                   name="hourlyWage2"
                                   placeholder="10030"
                                   value="">
                        </div>

                        <!-- ✅ 주휴수당 적용: 시급 옆 다음 위치 -->
                        <div class="info-item">
                            <div class="label">주휴수당 적용</div>
                            <div class="form-check form-switch mt-1">
                                <input class="form-check-input"
                                       type="checkbox"
                                       role="switch"
                                       id="includeWeeklyHolidayAllowance"
                                       name="includeWeeklyHolidayAllowance">
                                <label class="form-check-label text-xs text-muted" for="includeWeeklyHolidayAllowance">
                                    주 15시간 이상 근무 시 자동 계산
                                </label>
                            </div>
                        </div>
                    </div>

                    <!-- 3줄: 급여지급일 / 은행 / 계좌 / 세금 적용 -->
                    <div class="info-grid mb-3">
                        <!-- ✅ 급여지급일을 이 줄 맨 앞으로 이동 -->
                        <div class="info-item">
                            <div class="label">급여지급일</div>
                            <select class="form-select form-select-sm"
                                    name="payday"
                                    required>
                                <option value="">선택</option>
                                <option value="EOM">말일</option>
                                <!-- 🔹 1 ~ 31일까지 모두 표시 -->
                                <option value="1">1일</option>
                                <option value="2">2일</option>
                                <option value="3">3일</option>
                                <option value="4">4일</option>
                                <option value="5">5일</option>
                                <option value="6">6일</option>
                                <option value="7">7일</option>
                                <option value="8">8일</option>
                                <option value="9">9일</option>
                                <option value="10">10일</option>
                                <option value="11">11일</option>
                                <option value="12">12일</option>
                                <option value="13">13일</option>
                                <option value="14">14일</option>
                                <option value="15">15일</option>
                                <option value="16">16일</option>
                                <option value="17">17일</option>
                                <option value="18">18일</option>
                                <option value="19">19일</option>
                                <option value="20">20일</option>
                                <option value="21">21일</option>
                                <option value="22">22일</option>
                                <option value="23">23일</option>
                                <option value="24">24일</option>
                                <option value="25">25일</option>
                                <option value="26">26일</option>
                                <option value="27">27일</option>
                                <option value="28">28일</option>
                                <option value="29">29일</option>
                                <option value="30">30일</option>
                                <option value="31">31일</option>
                            </select>
                        </div>

                        <!-- ✅ 그다음 은행 -->
                        <div class="info-item">
                            <div class="label">은행</div>
                            <!-- 🔹 은행 리스트 select 로 변경 -->
                            <select class="form-select form-select-sm"
                                    name="bankName">
                                <option value="">선택</option>
                                <option value="KB국민은행">KB국민은행</option>
                                <option value="신한은행">신한은행</option>
                                <option value="하나은행">하나은행</option>
                                <option value="우리은행">우리은행</option>
                                <option value="NH농협은행">NH농협은행</option>
                                <option value="IBK기업은행">IBK기업은행</option>
                                <option value="SC제일은행">SC제일은행</option>
                                <option value="씨티은행">씨티은행</option>
                                <option value="카카오뱅크">카카오뱅크</option>
                                <option value="토스뱅크">토스뱅크</option>
                                <option value="부산은행">부산은행</option>
                                <option value="경남은행">경남은행</option>
                                <option value="대구은행">대구은행</option>
                                <option value="광주은행">광주은행</option>
                                <option value="전북은행">전북은행</option>
                                <option value="제주은행">제주은행</option>
                            </select>
                        </div>

                        <!-- ✅ 그다음 계좌번호 -->
                        <div class="info-item">
                            <div class="label">계좌번호</div>
                            <input type="text"
                                   class="form-control form-control-sm"
                                   name="bankAccount"
                                   value="">
                        </div>

                        <!-- ✅ 마지막 세금 적용 -->
                        <div class="info-item">
                            <div class="label">세금 적용</div>
                            <div class="form-check form-switch mt-1">
                                <input class="form-check-input"
                                       type="checkbox"
                                       role="switch"
                                       id="applyTax"
                                       name="applyTax">
                                <label class="form-check-label text-xs text-muted" for="applyTax">
                                    3.3% 등 소득세 공제 적용
                                </label>
                            </div>
                        </div>
                    </div>

                    <!-- 4줄: 보건증 + 예상 급여 버튼 -->
                    <div class="info-grid mb-4">
                        <div class="info-item">
                            <div class="label">보건증 보유 여부</div>
                            <div class="form-check form-switch mt-1">
                                <input class="form-check-input"
                                       type="checkbox"
                                       role="switch"
                                       id="hasHealthCertificate"
                                       name="hasHealthCertificate">
                                <label class="form-check-label text-xs text-muted" for="hasHealthCertificate">
                                    보건증 소지
                                </label>
                            </div>
                        </div>

                        <div class="info-item">
                            <div class="label">보건증 유효기간</div>
                            <div class="d-flex align-items-center gap-2">
                                <input type="date"
                                       class="form-control form-control-sm"
                                       name="healthCertExpiry"
                                       id="healthCertExpiry"
                                       value="">
                            </div>
                        </div>

                        <div class="info-item"></div>

                        <div class="info-item">
                            <button type="button"
                                    id="btnOpenPayEstimate"
                                    class="btn btn-sm btn-outline-primary mt-3">
                                예상 급여 보기
                            </button>
                        </div>
                    </div>

                    <!-- 메모 -->
                    <div class="info-grid mb-4">
                        <div class="info-item">
                            <div class="label">메모</div>
                            <textarea class="form-control form-control-sm"
                                      name="memo"
                                      rows="2"
                                      placeholder="간단 메모를 남겨두세요."></textarea>
                        </div>
                    </div>

                    <!-- ============ 주간 템플릿 편집 (같은 패널 안의 섹션) ============ -->
                    <section class="panel-schedule mb-3 mt-4">

                        <!-- 🔵 주간 템플릿 헤더 전체 파란 영역 (제목 + 슬롯 버튼 + 설명) -->
                        <header class="panel-schedule-header-blue">
                            <div class="d-flex flex-wrap justify-content-between align-items-start gap-2">
                                <div>
                                    <div class="title-sm schedule-header-kicker">SCHEDULE TEMPLATE</div>
                                    <h3 class="title-md mb-1 text-white">
                                        아르바이트 — 주간 템플릿 (월~일)
                                    </h3>
                                    <div class="text-xs schedule-header-text">
                                        10/30/60분 단위 · 드래그로 선택/해제<br>
                                        <span class="schedule-header-strong">폼 저장 시 현재 템플릿이 함께 저장됩니다.</span>
                                    </div>
                                </div>

                                <!-- 🔵 슬롯 단위 & 버튼 툴바 (파란 헤더 안) -->
                                <div class="panel-schedule-toolbar d-flex flex-column align-items-end gap-2">
                                    <div class="d-flex flex-wrap align-items-center justify-content-end gap-2">
                                        <span class="text-xs schedule-header-text">슬롯 단위</span>

                                        <!-- ✅ 라운드 직사각형 세그먼트 버튼 -->
                                        <div class="slot-toggle-group" id="gridSlotButtons">
                                            <button type="button" class="slot-toggle-btn active" data-grid-slot="10">10분</button>
                                            <button type="button" class="slot-toggle-btn" data-grid-slot="30">30분</button>
                                            <button type="button" class="slot-toggle-btn" data-grid-slot="60">1시간</button>
                                        </div>

                                        <button type="button"
                                                class="btn btn-sm btn-outline-light subtle-outline-btn"
                                                id="btnGridClear">
                                            초기화
                                        </button>
                                    </div>

                                    <div class="text-xs text-end schedule-header-text">
                                        마우스를 드래그하거나 클릭하여 <strong>주간 템플릿(계획)</strong>을 설정하세요.<br>
                                        다시 선택하면 <span class="schedule-header-strong">해제</span>됩니다.
                                    </div>
                                </div>
                            </div>
                        </header>

                        <div class="panel-schedule-body">
                            <div class="schedule-board">
                                <div class="table-responsive schedule-table-wrap">
                                    <table class="table schedule-table text-center" id="tableWeek10m">
                                        <thead>
                                        <tr id="theadWeek">
                                            <th class="time-col">시간</th>
                                            <th>월</th>
                                            <th>화</th>
                                            <th>수</th>
                                            <th>목</th>
                                            <th>금</th>
                                            <th>토</th>
                                            <th>일</th>
                                        </tr>
                                        </thead>
                                        <tbody id="tbodyWeek10m"></tbody>
                                    </table>
                                </div>
                                <div class="legend-row text-xs text-muted mt-2">
                                    <span class="legend-box legend-plan"></span> 선택된 템플릿 셀
                                </div>
                            </div>
                        </div>
                    </section>
                </form>

            </div>
        </section>

    </div>
</main>

<style>
    /* ===== 전체 배경/레이아웃 ===== */
    body {
        background:
                radial-gradient(circle at top left, #f5f7ff 0, #eef3ff 36%, transparent 70%),
                radial-gradient(circle at bottom right, #fef6f3 0, #ffe8da 40%, #ffe1f0 75%);
    }
    .schedule-shell {
        min-height: calc(100vh - 80px);
    }

    .schedule-layout {
        display: flex;
        flex-direction: column;
        gap: 1.2rem;
    }

    .panel {
        border-radius: 18px;
        background: rgba(255,255,255,0.9);
        box-shadow: 0 18px 40px rgba(15,23,42,0.08);
        border: 1px solid rgba(148,163,184,0.18);
        backdrop-filter: blur(14px);
        -webkit-backdrop-filter: blur(14px);
    }

    .panel-body {
        padding: 0.9rem 1.3rem 1.2rem 1.3rem;
    }

    .panel-merged .panel-body-merged {
        padding-top: 1.2rem;
    }

    /* ===== 🔵 상단 아르바이트 정보 입력 파란 헤더 ===== */
    .status-panel-blue {
        background: linear-gradient(135deg, #1d4ed8, #2563eb);
        border-radius: 18px 18px 0 0;
        padding: 16px 20px 18px;
        box-shadow: 0 14px 30px rgba(15, 23, 42, 0.28);
        color: #e5eeff;
    }
    .member-header-blue {
        margin: -1px -1px 0 -1px; /* 패널 테두리와 자연스럽게 붙도록 */
    }

    .status-title {
        font-size: 1.1rem;
        font-weight: 700;
        letter-spacing: .02em;
    }
    .status-subtitle-small {
        font-size: .8rem;
        margin-top: 2px;
        color: rgba(226, 232, 255, 0.78);
    }
    .status-period-label-caption {
        font-size: .75rem;
        color: rgba(226, 232, 255, 0.9);
    }

    .status-select-top {
        background: rgba(15, 23, 42, 0.2);
        border-color: rgba(191, 219, 254, 0.9);
        color: #e5eeff;
        min-width: 120px;
    }
    .status-select-top:focus {
        background: rgba(15, 23, 42, 0.35);
        border-color: #bfdbfe;
        box-shadow: 0 0 0 0.18rem rgba(191, 219, 254, 0.45);
        color: #f9fbff;
    }
    .status-select-top option {
        color: #111827;
    }

    .status-name-input {
        min-width: 160px;
        background: rgba(15, 23, 42, 0.15);
        border-color: rgba(191,219,254,0.9);
        color: #e5eeff;
    }
    .status-name-input::placeholder {
        color: rgba(226,232,255,0.8);
    }
    .status-name-input:focus {
        background: rgba(15,23,42,0.25);
        border-color: #bfdbfe;
        box-shadow: 0 0 0 0.18rem rgba(191,219,254,0.45);
        color: #f9fbff;
    }

    .member-save-btn {
        border-radius: 999px;
        padding-inline: 14px;
    }

    .title-sm {
        font-size: 0.7rem;
        letter-spacing: .16em;
        text-transform: uppercase;
        color: #cbd5f5;
        font-weight: 600;
    }
    .title-md {
        font-size: 0.98rem;
        font-weight: 600;
        color: #111827;
    }
    .text-xs {
        font-size: 0.75rem;
    }

    .info-grid {
        display:grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap:0.9rem 1.2rem;
    }
    .info-item .label {
        font-size:0.7rem;
        text-transform:uppercase;
        letter-spacing:.12em;
        color:#9ca3af;
        margin-bottom:0.25rem;
    }

    .form-control-sm,
    .form-select-sm {
        font-size:0.82rem;
        padding:.25rem .5rem;
        border-radius:0.6rem;
    }

    .form-control-sm:focus,
    .form-select-sm:focus {
        box-shadow:0 0 0 1px rgba(59,130,246,0.3);
        border-color:#2563eb;
    }

    /* ===== 주간 템플릿 섹션 (카드 느낌, 같은 패널 내부) ===== */
    .panel-schedule {
        border-radius: 14px;
        border: 1px solid rgba(226,232,240,0.9);
        background: rgba(248,250,252,0.8);
        padding: 0;
        overflow: hidden;
    }

    /* 🔵 주간템플릿 파란 헤더 */
    .panel-schedule-header-blue {
        background: linear-gradient(135deg, #1d4ed8, #2563eb);
        padding: 0.9rem 1rem 0.9rem 1rem;
        border-bottom: 1px solid rgba(191, 219, 254, 0.7);
        color: #e5eeff;
    }
    .schedule-header-kicker {
        color: rgba(226,232,255,0.85);
    }
    .schedule-header-text {
        color: rgba(226,232,255,0.85);
    }
    .schedule-header-strong {
        color: #facc15;
    }
    .panel-schedule-toolbar {
        min-width: 260px;
    }

    .panel-schedule-body {
        padding: 0.8rem 0.9rem 0.9rem;
    }

    /* 🔵 슬롯 토글 세그먼트 버튼 */
    .slot-toggle-group {
        display: inline-flex;
        align-items: center;
        border-radius: 999px;
        padding: 2px;
        background: rgba(15, 23, 42, 0.18);
        box-shadow: 0 0 0 1px rgba(191, 219, 254, 0.5);
        gap: 2px;
    }

    .slot-toggle-btn {
        border: none;
        outline: none;
        background: transparent;
        color: rgba(226, 232, 255, 0.85);
        font-size: 0.78rem;
        font-weight: 500;
        padding: 0.25rem 0.8rem;
        border-radius: 999px;
        min-width: 56px;
        cursor: pointer;
        transition:
                background 0.15s ease,
                color 0.15s ease,
                box-shadow 0.15s ease,
                transform 0.06s ease;
        white-space: nowrap;
    }

    .slot-toggle-btn:hover {
        background: rgba(15, 23, 42, 0.28);
        color: #ffffff;
        transform: translateY(-0.3px);
    }

    .slot-toggle-btn.active {
        background: #0f172a;
        color: #f9fafb;
        box-shadow: 0 6px 14px rgba(15, 23, 42, 0.45);
    }

    /* 🔹 파란헤더 안의 보조 버튼(초기화/원래대로)을 조금 더 잔잔하게 */
    .subtle-outline-btn {
        border-radius: 999px;
        border-color: rgba(191, 219, 254, 0.85);
        color: rgba(226, 232, 255, 0.9);
        background: transparent;
        font-size: 0.76rem;
        padding-inline: 0.7rem;
    }
    .subtle-outline-btn:hover {
        background: rgba(15, 23, 42, 0.25);
        color: #ffffff;
    }

    /* ====== 🔁 SCHEDULE 타임 그리드: 기존 EDIT 페이지와 동일 스타일 ====== */
    .schedule-board {
        border-radius: 10px;
        border: 1px solid #e5e7eb;
        background: #ffffff;
        overflow: hidden;
        padding: 0.6rem 0.6rem 0.7rem;
    }
    .schedule-table-wrap {
        max-height: 520px;
        overflow: auto;
    }

    .schedule-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
        table-layout: fixed;
        font-size: 0.78rem;
        margin-bottom: 0;
    }

    /* 헤더: 파란색 단색 배경 + sticky */
    .schedule-table thead th {
        background: #2563eb;
        color:#e5eeff;
        font-weight: 600;
        font-size: 0.9rem;
        border-bottom: 1px solid #1d4ed8;
        text-align: center;
        padding: .5rem .4rem;
        position: sticky;
        top: 0;
        z-index: 2;
    }

    .schedule-table th,
    .schedule-table td {
        border: 0;
        padding: 2px 3px;
        vertical-align: middle;
    }

    /* 왼쪽 시간 컬럼: 회색 배경 + sticky */
    .schedule-table .time-col {
        background: #fafafa;
        color:#6b7280;
        font-weight: 600;
        width: 74px;
        border-right: 1px solid #e5e7eb;
        text-align: center;
        padding: .25rem .25rem;
        font-size: .8rem;
        position: sticky;
        left: 0;
        z-index: 3;
        font-variant-numeric: tabular-nums;
    }

    .cell-10m {
        height: 18px;
        margin: 1px 0;
        border-radius: 6px;
        border: 1px solid #eef2f6;
        background: #ffffff;
        cursor: pointer;
        user-select: none;
        transition: background .15s ease,
        box-shadow .15s ease,
        border-color .15s ease,
        transform .08s ease;
    }

    .cell-10m:hover {
        background: #f8fbff;
        box-shadow: 0 0 0 1px rgba(148,163,184,0.35);
        transform: translateY(-0.5px);
    }

    .cell-10m.schedule-on {
        background: rgba(102, 187, 106, 0.55);
        border-color: rgba(46, 160, 67, 0.6);
        box-shadow: inset 0 0 0 1px rgba(46, 160, 67, 0.6);
    }

    /* 매 정시마다 진한 가로 경계선 */
    tr.hour-start > td,
    tr.hour-start > th {
        border-top: 2px solid #e5e7eb !important;
    }

    .legend-row {
        display:flex;
        align-items:center;
        gap:0.5rem;
        flex-wrap:wrap;
        margin-top: 0.5rem;
    }
    .legend-box {
        display:inline-block;
        width:14px;
        height:14px;
        border-radius:4px;
        border:1px solid rgba(209,213,219,0.9);
    }
    .legend-plan {
        background: rgba(102,187,106,0.55);
        border-color: rgba(46,160,67,0.6);
    }

    @media (max-width: 768px) {
        .schedule-layout {
            gap:0.8rem;
        }

        .status-panel-blue {
            padding: 14px 14px 16px;
        }

        .status-inline-row {
            flex-direction: column;
            align-items: flex-start;
        }

        .panel-schedule-header-blue {
            padding: 0.8rem 0.8rem;
        }

        .panel-schedule-toolbar {
            align-items: flex-start !important;
        }

        .slot-toggle-group {
            width: 100%;
            justify-content: space-between;
        }
        .slot-toggle-btn {
            flex: 1 1 0;
            text-align: center;
            padding-inline: 0.4rem;
        }
    }
</style>

<!-- 🔹 memberJson 없이도 동작: 그냥 빈 JSON (현재는 사용 안 하지만 남겨둠) -->
<script id="memberData" type="application/json">
    {}
</script>

<script id="memberData" type="application/json">
    {}
</script>

<script>
    (function () {
        const HOUR_START = 6;
        const HOUR_END   = 22;
        const DAY_CODES  = ['MON','TUE','WED','THU','FRI','SAT','SUN'];
        const DOW_LABELS = ['월','화','수','목','금','토','일'];

        const tbodyWeek       = document.getElementById('tbodyWeek10m');
        const theadWeek       = document.getElementById('theadWeek');
        const gridSlotButtons = document.getElementById('gridSlotButtons');
        const btnGridClear    = document.getElementById('btnGridClear');
        const form            = document.getElementById('memberEditForm');

        let gridStepMinutes   = 10;
        let scheduleMapByDay  = buildEmptyScheduleMap();   // 현재 스케줄 상태
        let gridDragging      = false;
        let gridDragAdding    = true;

        // 초기: 10분 단위 그리드 생성
        buildWeekGrid();
        renderWeekHeadDays();
        paintScheduleFromMap(scheduleMapByDay);

        attachCellDragHandlers();
        attachSlotButtons();
        attachFormSubmitHandler();
        attachResetButtons();

        /* ================== 기본 맵/그리드 ================== */
        function buildEmptyScheduleMap() {
            const map = {};
            DAY_CODES.forEach(dc => {
                map[dc] = [];
            });
            return map;
        }

        function buildWeekGrid() {
            tbodyWeek.innerHTML = '';

            const totalMinutes = (HOUR_END - HOUR_START) * 60;
            const totalRows    = totalMinutes / gridStepMinutes;

            for (let idx = 0; idx < totalRows; idx++) {
                const absMin = HOUR_START * 60 + idx * gridStepMinutes;
                const h = Math.floor(absMin / 60);
                const m = absMin % 60;
                const hm = pad2(h) + ':' + pad2(m);

                const tr = document.createElement('tr');
                if (m === 0) tr.classList.add('hour-start');

                let tds = '';
                for (let col = 1; col <= 7; col++) {
                    tds += `<td data-col="${col}">
                                <div class="cell-10m" data-row="${idx}" data-day-index="${col-1}"></div>
                            </td>`;
                }

                tr.innerHTML = `
                    <th class="timecell time-col" scope="row">${hm}</th>
                    ${tds}
                `;
                tbodyWeek.appendChild(tr);
            }
        }

        function renderWeekHeadDays() {
            const ths = theadWeek.querySelectorAll('th');
            for (let i = 0; i < 7; i++) {
                const th = ths[i + 1];
                if (!th) continue;
                th.textContent = DOW_LABELS[i];
            }
        }

        function paintScheduleFromMap(map) {
            tbodyWeek.querySelectorAll('.cell-10m').forEach(c => c.classList.remove('schedule-on'));

            const rows = Array.from(tbodyWeek.querySelectorAll('tr'));

            for (let dayIdx = 0; dayIdx < 7; dayIdx++) {
                const dayCode = DAY_CODES[dayIdx];
                const segs = map[dayCode] || [];
                if (!segs.length) continue;

                rows.forEach((tr, rIdx) => {
                    const cellDiv = tr.querySelector(`td[data-col="${dayIdx+1}"] .cell-10m`);
                    if (!cellDiv) return;

                    const { startMin, endMin } = rowToInterval(rIdx);

                    const overlaps = segs.some(seg => {
                        const ss = hmToMin(seg.start);
                        const ee = hmToMin(seg.end);
                        return !(ee <= startMin || ss >= endMin);
                    });

                    if (overlaps) {
                        cellDiv.classList.add('schedule-on');
                    }
                });
            }
        }

        function rowToInterval(rowIdx) {
            const startMin = HOUR_START * 60 + rowIdx * gridStepMinutes;
            const endMin   = startMin + gridStepMinutes;
            return { startMin, endMin };
        }

        function hmToMin(hm) {
            const [hStr, mStr] = String(hm).split(':');
            const h = parseInt(hStr || '0', 10);
            const m = parseInt(mStr || '0', 10);
            return h * 60 + m;
        }

        function pad2(n) { return String(n).padStart(2, '0'); }

        function minToHm(min) {
            const h = Math.floor(min / 60);
            const m = min % 60;
            return pad2(h) + ':' + pad2(m) + ':00';
        }

        function clearGridSelection() {
            tbodyWeek.querySelectorAll('.cell-10m').forEach(c => {
                c.classList.remove('schedule-on');
            });
        }

        /* ================== 드래그/클릭 편집 ================== */
        function attachCellDragHandlers() {
            if (!tbodyWeek) return;

            // 이벤트 위임 방식이라, 그리드 다시 그려져도 계속 동작
            tbodyWeek.addEventListener('mousedown', (e) => {
                const cell = e.target.closest('.cell-10m');
                if (!cell || !tbodyWeek.contains(cell)) return;

                e.preventDefault();

                gridDragging = true;
                const nowOn  = cell.classList.contains('schedule-on');
                const nextOn = !nowOn;
                gridDragAdding = nextOn;

                toggleCell(cell, nextOn);
            });

            tbodyWeek.addEventListener('mouseover', (e) => {
                if (!gridDragging) return;
                const cell = e.target.closest('.cell-10m');
                if (!cell || !tbodyWeek.contains(cell)) return;
                toggleCell(cell, gridDragAdding);
            });

            window.addEventListener('mouseup', () => {
                gridDragging = false;
            }, { passive: true });
        }

        function toggleCell(cell, on) {
            if (on) cell.classList.add('schedule-on');
            else    cell.classList.remove('schedule-on');
        }

        /* ================== 슬롯 단위 변경(10/30/60) ================== */
        function attachSlotButtons() {
            if (!gridSlotButtons) return;

            gridSlotButtons.querySelectorAll('button[data-grid-slot]').forEach(btn => {
                btn.addEventListener('click', () => {
                    const mins = parseInt(btn.dataset.gridSlot, 10);
                    if (!mins || mins === gridStepMinutes) return;

                    // 현재 그리드에서 선택 상태를 먼저 map으로 반영
                    scheduleMapByDay = collectSegmentsFromGrid();

                    // 활성화 버튼 UI 갱신
                    gridSlotButtons.querySelectorAll('button[data-grid-slot]')
                            .forEach(b => b.classList.remove('active'));
                    btn.classList.add('active');

                    // 슬롯 단위 변경
                    gridStepMinutes = mins;

                    // 새로운 슬롯 단위로 그리드 재구성 + 기존 스케줄 다시 칠하기
                    buildWeekGrid();
                    renderWeekHeadDays();
                    paintScheduleFromMap(scheduleMapByDay);
                });
            });
        }

        /* ================== 초기화 버튼 ================== */
        function attachResetButtons() {
            if (btnGridClear) {
                btnGridClear.addEventListener('click', () => {
                    clearGridSelection();
                    // 선택을 모두 지우고 스케줄 상태도 비우기
                    scheduleMapByDay = buildEmptyScheduleMap();
                });
            }
        }

        /* ================== submit 시 hidden 필드 생성 ================== */
        function normalizeBooleanCheckbox(id, fieldName) {
            if (!form) return;
            const cb = document.getElementById(id);
            if (!cb) return;

            Array.from(form.querySelectorAll('input[type="hidden"][name="' + fieldName + '"]'))
                    .forEach(el => el.remove());

            if (cb.hasAttribute('name')) {
                cb.removeAttribute('name');
            }

            appendHidden(fieldName, cb.checked ? 'true' : 'false');
        }

        function attachFormSubmitHandler() {
            if (!form) return;

            form.addEventListener('submit', () => {
                normalizeBooleanCheckbox('includeWeeklyHolidayAllowance', 'includeWeeklyHolidayAllowance');
                normalizeBooleanCheckbox('hasHealthCertificate', 'hasHealthCertificate');
                normalizeBooleanCheckbox('applyTax', 'applyTax');

                Array.from(form.querySelectorAll('input[name^="schedule["]')).forEach(el => el.remove());

                const daySegMap = collectSegmentsFromGrid();

                let idx = 0;
                DAY_CODES.forEach(dayCode => {
                    const segs = daySegMap[dayCode] || [];
                    segs.forEach(seg => {
                        appendHidden(`schedule[${idx}].day`,   dayCode);
                        appendHidden(`schedule[${idx}].start`, seg.start);
                        appendHidden(`schedule[${idx}].end`,   seg.end);
                        idx++;
                    });
                });
            });
        }

        /* ================== 그리드 → 구간 맵으로 변환 ================== */
        function collectSegmentsFromGrid() {
            const result = {};
            DAY_CODES.forEach(dc => result[dc] = []);

            if (!tbodyWeek) return result;

            const rows = Array.from(tbodyWeek.querySelectorAll('tr'));

            for (let dayIdx = 0; dayIdx < 7; dayIdx++) {
                const indices = [];

                rows.forEach((tr, rIdx) => {
                    const cellDiv = tr.querySelector(`td[data-col="${dayIdx+1}"] .cell-10m`);
                    if (cellDiv && cellDiv.classList.contains('schedule-on')) {
                        indices.push(rIdx);
                    }
                });

                if (!indices.length) continue;

                const merged = mergeConsecutive(indices);
                const dayCode = DAY_CODES[dayIdx];

                merged.forEach(([sIdx, eIdx]) => {
                    const startMin = HOUR_START * 60 + sIdx * gridStepMinutes;
                    const endMin   = HOUR_START * 60 + (eIdx + 1) * gridStepMinutes;

                    result[dayCode].push({
                        start: minToHm(startMin),
                        end:   minToHm(endMin)
                    });
                });
            }

            // 현재 상태를 유지
            scheduleMapByDay = result;
            return result;
        }

        function mergeConsecutive(arr) {
            if (!arr.length) return [];
            const sorted = arr.slice().sort((a,b) => a - b);
            const out = [];
            let s = sorted[0];
            let prev = sorted[0];

            for (let i = 1; i < sorted.length; i++) {
                const v = sorted[i];
                if (v === prev + 1) {
                    prev = v;
                    continue;
                }
                out.push([s, prev]);
                s = prev = v;
            }
            out.push([s, prev]);
            return out;
        }

        function appendHidden(name, value) {
            const input = document.createElement('input');
            input.type  = 'hidden';
            input.name  = name;
            input.value = value;
            form.appendChild(input);
        }

    })();

    // ===== 전화번호 자동 포맷 (010-0000-0000 등) =====
    document.addEventListener('DOMContentLoaded', function () {
        const phoneInput = document.getElementById('phone');
        if (phoneInput) {
            phoneInput.addEventListener('input', function () {
                const cursorPos = this.selectionStart;
                const beforeLength = this.value.length;

                this.value = formatKoreanPhone(this.value);

                const afterLength = this.value.length;
                const diff = afterLength - beforeLength;
                this.setSelectionRange(cursorPos + diff, cursorPos + diff);
            });

            phoneInput.addEventListener('blur', function () {
                this.value = formatKoreanPhone(this.value);
            });
        }

        // 예상 급여 버튼: 신규등록에서는 항상 경고
        const btn = document.getElementById('btnOpenPayEstimate');
        if (btn) {
            btn.addEventListener('click', function () {
                const memberId = ''; // 신규 등록 화면에서는 ID 없음

                if (!memberId) {
                    alert('예상 급여 조회는 아르바이트 정보 저장 후 이용 가능합니다.');
                    return;
                }

                const today = new Date();

                const monday = new Date(today.getFullYear(), today.getMonth(), today.getDate());
                const day    = monday.getDay();
                const offset = (day + 6) % 7;
                monday.setDate(monday.getDate() - offset);

                const sunday = new Date(monday.getFullYear(), monday.getMonth(), monday.getDate() + 6);

                function pad2(n) {
                    return String(n).padStart(2, '0');
                }
                function toIso(d) {
                    return d.getFullYear() + '-' + pad2(d.getMonth() + 1) + '-' + pad2(d.getDate());
                }

                const weekStart = toIso(monday);
                const weekEnd   = toIso(sunday);

                const url =
                        '/members/pay-estimate'
                        + '?memberId=' + encodeURIComponent(memberId)
                        + '&weekStart=' + encodeURIComponent(weekStart)
                        + '&weekEnd=' + encodeURIComponent(weekEnd);

                window.open(
                        url,
                        'payEstimatePopup',
                        'width=1200,height=900,scrollbars=yes,resizable=yes'
                );
            });
        }
    });

    // 한국 전화번호 포맷 함수
    function formatKoreanPhone(value) {
        // 숫자만 추출
        let digits = String(value).replace(/\D/g, '');

        if (digits.length === 0) return '';

        // 02로 시작하는 경우(서울)
        if (digits.startsWith('02')) {
            if (digits.length <= 2) {
                return digits;
            } else if (digits.length <= 5) {
                // 02-XXX
                return digits.slice(0, 2) + '-' + digits.slice(2);
            } else if (digits.length <= 9) {
                // 02-XXX-XXXX
                return digits.slice(0, 2) + '-' + digits.slice(2, digits.length - 4) + '-' + digits.slice(-4);
            } else {
                // 02-XXXX-XXXX (자릿수 초과 시 앞을 늘리고 뒤 4자리 고정)
                return digits.slice(0, 2) + '-' + digits.slice(2, digits.length - 4) + '-' + digits.slice(-4);
            }
        }

        // 휴대폰 / 일반 3자리 국번 (010, 011, 031 등)
        if (digits.length <= 3) {
            return digits;
        } else if (digits.length <= 7) {
            // 010-1234
            return digits.slice(0, 3) + '-' + digits.slice(3);
        } else if (digits.length <= 11) {
            // 010-1234-5678 또는 031-123-4567 등
            return digits.slice(0, 3) + '-' + digits.slice(3, digits.length - 4) + '-' + digits.slice(-4);
        } else {
            // 너무 긴 경우는 뒤 11자리만 사용
            digits = digits.slice(-11);
            return digits.slice(0, 3) + '-' + digits.slice(3, 7) + '-' + digits.slice(7);
        }
    }
</script>

{{>layouts/footer}}
