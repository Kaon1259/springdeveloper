<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{#pageTitle}}{{pageTitle}}{{/pageTitle}}{{^pageTitle}}Cafe Friends Manager - 로그인{{/pageTitle}}</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
          rel="stylesheet" crossorigin="anonymous">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            crossorigin="anonymous"></script>

    <!-- Google Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --cafe-bg: #fff7ec;
            --cafe-bg-accent: #fde7c7;
            --cafe-brown: #5b3a29;
            --cafe-brown-dark: #3d261a;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Noto Sans KR', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: radial-gradient(circle at top left, var(--cafe-bg-accent), var(--cafe-bg));
            min-height: 100vh;
            color: var(--cafe-brown);
        }

        /* 로딩 오버레이 */
        #loading-overlay {
            position: fixed;
            inset: 0;
            background: rgba(255, 247, 236, 0.9);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            transition: opacity 0.4s ease;
        }

        #loading-overlay.hidden {
            opacity: 0;
            pointer-events: none;
        }

        .spinner-cup {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            border: 4px solid #f0d2aa;
            border-top-color: var(--cafe-brown);
            animation: spin 0.9s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* 메인 컨텐츠 레이아웃 */
        .main-wrapper {
            min-height: 100vh;
        }

        .welcome-card {
            max-width: 520px;
            width: 100%;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            box-shadow:
                    0 18px 45px rgba(0, 0, 0, 0.08),
                    0 0 0 1px rgba(255, 255, 255, 0.8);
            padding: 2.4rem 2.25rem 2rem;
            animation: fadeUp 0.7s ease-out both;
        }

        @keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(18px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .main-logo {
            max-width: 260px;
            border-radius: 14px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12);
            animation: fadeIn 0.9s ease-out both;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.95);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .welcome-title {
            color: var(--cafe-brown-dark);
            letter-spacing: 0.03em;
        }

        .welcome-sub {
            color: #8a5e43;
        }

        /* 이메일 로그인 폼 */
        .login-form-group label {
            font-size: 0.9rem;
            color: #6b4c36;
            margin-bottom: 0.25rem;
        }

        .login-form-group .form-control {
            border-radius: 999px;
            border: 1px solid #e2d3c3;
            padding: 0.45rem 1rem;
            font-size: 0.95rem;
        }

        .login-form-group .form-control:focus {
            border-color: #b87336;
            box-shadow: 0 0 0 0.15rem rgba(184, 115, 54, 0.25);
        }

        .btn-email-login {
            background: linear-gradient(135deg, #b87336, #8b4a24);
            border: none;
            color: #fff;
            font-weight: 600;
            font-size: 0.98rem;
            padding: 0.6rem 1.5rem;
            border-radius: 999px;
            width: 100%;
            box-shadow: 0 8px 20px rgba(139, 74, 36, 0.30);
            transition:
                    transform 0.15s ease-out,
                    box-shadow 0.15s ease-out,
                    filter 0.15s ease-out;
        }

        .btn-email-login:hover,
        .btn-email-login:focus {
            transform: translateY(-1px);
            box-shadow: 0 12px 26px rgba(139, 74, 36, 0.40);
            filter: brightness(1.03);
        }

        .btn-email-login:active {
            transform: translateY(0);
            box-shadow: 0 5px 14px rgba(139, 74, 36, 0.32);
        }

        /* 구분선 */
        .divider {
            display: flex;
            align-items: center;
            margin: 1.25rem 0 1rem;
            color: #b08a6b;
            font-size: 0.85rem;
        }

        .divider::before,
        .divider::after {
            content: "";
            flex: 1;
            border-top: 1px dashed #e2d3c3;
        }

        .divider span {
            padding: 0 0.75rem;
        }

        /* 소셜 로그인 버튼 */
        .btn-social {
            width: 100%;
            border-radius: 999px;
            padding: 0.55rem 1rem;
            font-weight: 500;
            font-size: 0.95rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.45rem;
            margin-bottom: 0.5rem;
            border-width: 1px;
        }

        .btn-social-icon {
            width: 18px;
            height: 18px;
            border-radius: 4px;
            display: inline-block;
        }

        .btn-kakao {
            background-color: #FEE500;
            border-color: #FEE500;
            color: #3C1E1E;
        }

        .btn-kakao .btn-social-icon {
            background: #3C1E1E;
        }

        .btn-naver {
            background-color: #03C75A;
            border-color: #03C75A;
            color: #ffffff;
        }

        .btn-naver .btn-social-icon {
            background: #ffffff;
        }

        .btn-google {
            background-color: #ffffff;
            border-color: #e2e8f0;
            color: #334155;
        }

        .btn-google .btn-social-icon {
            background:
                    conic-gradient(from 0deg,
                    #4285F4 0deg 90deg,
                    #34A853 90deg 180deg,
                    #FBBC05 180deg 270deg,
                    #EA4335 270deg 360deg);
        }

        .btn-social:hover {
            filter: brightness(1.03);
            box-shadow: 0 8px 18px rgba(15, 23, 42, 0.12);
        }

        .btn-social:active {
            transform: translateY(0);
            box-shadow: 0 4px 10px rgba(15, 23, 42, 0.16);
        }

        /* 하단 링크 */
        .helper-text {
            font-size: 0.85rem;
            color: #b08a6b;
        }

        .link-muted {
            font-size: 0.85rem;
            color: #9c7a5d;
            text-decoration: none;
        }

        .link-muted:hover {
            color: #7b5a3e;
            text-decoration: underline;
        }

        .signup-link {
            font-weight: 600;
        }

        @media (max-width: 576px) {
            .welcome-card {
                margin: 1.5rem;
                padding: 1.8rem 1.5rem 1.4rem;
            }
            .main-logo {
                max-width: 220px;
            }
        }
    </style>
</head>
<body>

<!-- 로딩 애니메이션 -->
<div id="loading-overlay">
    <div class="d-flex flex-column align-items-center gap-3">
        <div class="spinner-cup"></div>
        <div class="fw-semibold" style="color: #8b5a35;">Cafe Friends Manager 로딩 중...</div>
    </div>
</div>

<div class="container-fluid main-wrapper d-flex align-items-center justify-content-center">
    <div class="welcome-card">
        <!-- 메인 타이틀 이미지 (클릭 시 /members 이동) -->
        <div class="text-center mb-3">
            <a href="/members" aria-label="근무자 관리 메인으로 이동">
                <img src="/img/mainTitle.jpg"
                     alt="Cafe Friends Manager"
                     class="img-fluid main-logo"
                     style="cursor:pointer;">
            </a>
        </div>

        <!-- 타이틀/설명 -->
        <div class="text-center mb-3">
            <h1 class="welcome-title h4 mb-1">
                Cafe Friends Manager
            </h1>
            <p class="welcome-sub mb-0">
                커피 프랜차이즈 동행 관리 프로그램에 로그인하세요.
            </p>
        </div>

        <!-- 이메일 로그인 폼 -->
        <form method="post" action="/login" class="mb-2">
            {{#_csrf}}
                <input type="hidden" name="{{parameterName}}" value="{{token}}">
            {{/_csrf}}

            <div class="login-form-group mb-2">
                <label for="email">이메일</label>
                <input type="email"
                       class="form-control"
                       id="email"
                       name="email"
                       placeholder="이메일 주소를 입력하세요"
                       required>
            </div>

            <div class="login-form-group mb-3">
                <label for="password">비밀번호</label>
                <input type="password"
                       class="form-control"
                       id="password"
                       name="password"
                       placeholder="비밀번호를 입력하세요"
                       required>
            </div>

            <button type="submit" class="btn btn-email-login">
                이메일로 로그인
            </button>

            <div class="d-flex justify-content-between mt-2">
                <a href="/forgot" class="link-muted">비밀번호를 잊으셨나요?</a>
            </div>
        </form>

        <!-- 구분선 -->
        <div class="divider">
            <span>또는 간편 로그인</span>
        </div>

        <!-- 소셜 로그인 버튼들 -->
        <div class="mb-2">
            <!-- 카카오 로그인 -->
            <a href="/oauth2/authorization/kakao" class="btn btn-social btn-kakao">
                <span class="btn-social-icon"></span>
                <span>카카오로 로그인</span>
            </a>

            <!-- 네이버 로그인 -->
            <a href="/oauth2/authorization/naver" class="btn btn-social btn-naver">
                <span class="btn-social-icon"></span>
                <span>네이버로 로그인</span>
            </a>

            <!-- 구글 로그인 -->
            <a href="/oauth2/authorization/google" class="btn btn-social btn-google">
                <span class="btn-social-icon"></span>
                <span>Google 계정으로 로그인</span>
            </a>
        </div>

        <!-- 회원가입 링크 -->
        <div class="text-center mt-3">
            <span class="helper-text d-block mb-1">
                아직 계정이 없으신가요?
            </span>
            <a href="/signup" class="link-muted signup-link">
                회원가입 하러 가기
            </a>
        </div>
    </div>
</div>

<script>
    // 페이지 로드 후 로딩 오버레이 제거
    window.addEventListener('load', function () {
        const overlay = document.getElementById('loading-overlay');
        if (!overlay) return;

        overlay.classList.add('hidden');
        setTimeout(function () {
            overlay.style.display = 'none';
        }, 400);
    });
</script>

</body>
</html>
